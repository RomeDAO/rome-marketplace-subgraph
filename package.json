{
  "name": "rome-marketplace-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ rome-marketplace-subgraph",
    "create-local": "graph create --node http://localhost:8020/ rome-marketplace-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ rome-marketplace-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rome-marketplace-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
